@model IEnumerable<Blog.Domain.Entities.Post>

@{
    ViewData["Title"] = "Publicaciones";
}

<h1>Lista de publicaciones</h1>

<hr/>

<p>
    <a asp-area="Admin" asp-controller="Posts" asp-action="Create" class="btn btn-primary"> <i class="ion-plus-circled"></i> Crear</a>
</p>


<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <!-- /.card-header -->
                    <div class="card-body">
                        <table id="example2" class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Title)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.User)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.PostStatus)
                                    </th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Title)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.User.Email)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.PostStatus.StatusName)
                                        </td>
                                        <td>
                                            <a asp-area="Admin" asp-controller="Posts" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary"> <i class="ion-edit"></i> Editar</a>
                                            <a asp-area="Admin" asp-controller="Posts" asp-action="Details" asp-route-id="@item.Id" class="btn btn-secondary"> <i class="ion-eye"></i> Detalles</a>
                                            <a asp-area="Admin" asp-controller="Posts" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger"> <i class="ion-trash-a"></i> Eliminar</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>